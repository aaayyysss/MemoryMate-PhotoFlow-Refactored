push for “smart gates”. What your logs show is not a tuning problem, it is a taxonomy problem: ViT-B/32 similarity scores overlap across “documents” and “normal photos”, so you need structural discriminators (hard filters) plus a small second stage re-rank (soft signals). That is exactly how the big apps behave: they combine semantic search with metadata and content signals (people, places, objects) rather than trusting one score.

Below is a concrete, step-by-step plan, tied to the files you already have (based on the snippets you pasted and the code in services/search_orchestrator.py that already contains _get_project_meta() and _detect_screenshot()).

Step-by-step: implement “Gate Profiles” per preset
Step 1: Introduce a single place where preset logic becomes “profile”

Goal: every preset becomes {retrieve → hard gates → soft boosts → final slice}. This is the iPhone, Google Photos, Lightroom mental model: a category is not just a query string, it is a pipeline.

Where to change

services/smart_find_service.py

Make each preset return a SearchProfile (or plain dict) with:

positive_prompts

negative_prompts

filters (media type, date, rating, flag)

gates (hard exclude and hard require)

boosts (soft scoring signals)

threshold (optional, but use sparingly)

Why
Right now presets behave mostly like “prompt bundles”. That will always leak.

Step 2: Add a GateEngine that runs after retrieval, before ranking

Goal: hard filters happen before scoring and before top_k slice, otherwise false acceptances remain and your ranking fights a losing battle.

Where to change

services/search_orchestrator.py

In _execute() (and also in search_ann(), find_similar(), search_metadata_only()), insert:

retrieve candidates

apply hard gates

score and sort

deduplicate

enforce unique paths

slice [:top_k]

You already have _enforce_unique_paths() and dedup logic, so gates should sit just before scoring.

Why
This is exactly what your “Documents” case needs: “exclude faces” and “exclude screenshots” must be hard gates, not just weak score factors.

Step 3: Fix “Documents” with a proper 3-part gate, not a threshold

You already discovered the core truth: thresholds do not separate documents vs portraits on ViT-B/32.

Documents: recommended pipeline

Retrieve (semantic, wide net)

Positive prompts: ["scanned document","printed page","form","invoice","receipt","handwritten note","paper","document","text"]

Negative prompts: ["selfie","portrait","person","face","group photo","landscape","mountain","beach","city","pet"]

Hard gates (structural)

exclude_faces: True
Use your existing face coverage from face_crops (you already fixed the table name, good).

exclude_screenshots: True
Use your existing _detect_screenshot() and meta["is_screenshot"].

require_document_visuals: True (new)
Implement a “document-likeness” check using lightweight visual features on thumbnails for the candidate set only, not the whole library:

high edge density (pages have lots of straight edges)

low colorfulness (documents tend to be gray/white background)

high text-likeness (optional OCR later)

This one is the missing piece that will eliminate “random photos with some texture” being accepted as documents.

Soft boosts (ranking)

boost if aspect ratio is paper-like:

A4-ish ratios (portrait ~ 0.70 to 0.78), receipts (very tall), screenshots usually 9:16 (but you already excluded them)

boost if EXIF indicates scan apps (later enhancement)

Where to implement

services/search_orchestrator.py

Extend _get_project_meta() to store:

is_screenshot (already present)

face_count (already present)

add doc_visual_score (new, computed lazily)

Add a new module:

services/document_vision.py (new)

compute_doc_visual_score(image_path, thumb_cache) -> float

Use thumbnail image from cache if available, otherwise load a small 256px version.

Best-practice alignment
This mirrors how major apps rely on multiple signals (semantic + content + metadata) instead of thresholding one similarity score.

Step 4: Screenshots should be filter-first, not CLIP-first

You already have _detect_screenshot() which uses filename patterns and known resolutions.

Recommended Screenshots profile

Retrieval: metadata only, no CLIP

Hard gates:

require_screenshot: True (meta flag)

Soft boosts:

prefer higher resolution

optionally prefer “recent” if users expect that

Where

services/smart_find_service.py

Set Screenshots preset to CLIP=False, filters=True

services/search_orchestrator.py

Ensure search_metadata_only() supports require_screenshot

Why
Screenshots are a taxonomy category in iPhone and Google Photos largely driven by device and file heuristics, not semantic embeddings.

Step 5: Mountains, Beach, City should add “diversity and sanity gates”

Your log shows category bleed: documents appear under Mountains (doc1.png, doc2.png). That is a classic embedding side effect on small libraries.

Mountains profile

Retrieval: CLIP prompts, allow backoff (already)

Hard gates:

exclude_documents: True (new)
This is the “inverse doc gate”: if doc_visual_score >= 0.7, drop it.

optional: exclude_screenshots: True

Soft boosts:

boost if has GPS (mountains often taken outdoors, GPS more common)

boost if wide aspect ratio (landscapes)

Where

services/search_orchestrator.py

Add generic gate hooks: exclude_by_doc_score, exclude_screenshots

Why
This is exactly how Lightroom and Excire feel “clean”: they do not just retrieve, they also suppress obvious cross-category artifacts.

Step 6: Wedding, Party need a “people gate” that is stricter than “face_presence”

Right now “Wedding” returns 1 result with face=1.0 and that is basically “any face photo”.

Wedding profile

Retrieval: CLIP prompts as you do

Hard gates:

require faces present, and ideally:

require face_count >= 2 for “party, group, wedding” (configurable)

Soft boosts:

boost if multiple faces

boost if date/time is evening (optional later)

Where

services/search_orchestrator.py

You already log face coverage for people-implied queries.

Add min_face_count in active profile, apply as a hard gate for these presets.

Why
In iPhone and Google Photos, “People-centric categories” behave like a people filter with semantics layered on top.

Concrete patch list by file
1) services/smart_find_service.py

Patch

Convert each preset into a profile with fields:

filters, positive_prompts, negative_prompts, gates, boosts, allow_backoff, threshold

Change:

Screenshots: CLIP=False, filters={"require_screenshot": True}

Documents: keep CLIP, but add gates:

exclude_faces=True

exclude_screenshots=True

require_doc_visual=True

Wedding, Party: gates min_face_count=2

2) services/search_orchestrator.py

Patch

Add apply_gates(candidates, profile, meta) and call it in:

_execute()

search_ann()

find_similar()

search_metadata_only()

Extend _get_project_meta() to include:

doc_visual_score (lazy compute, cached)

Add new gates:

require_doc_visual

exclude_documents

require_screenshot

min_face_count

3) services/document_vision.py (new)

Patch

Implement compute_doc_visual_score() using thumbnail-only features:

edge density

colorfulness

optional “textness” placeholder (OCR later)

Keep it fast and only run for candidates, not whole library.

4) services/semantic_search_service.py (optional refinement)

Patch

Support “negative prompt penalty” more explicitly if you do prompt fusion:

if an image matches negatives strongly, subtract from semantic score before gates
This reduces category bleed on small datasets.

5) Tests

tests/test_search_orchestrator.py

Add regression tests:

Documents excludes face_count > 0

Documents excludes screenshots

Mountains excludes high doc_visual_score

Wedding requires min_face_count

What I recommend you do first (order matters)

Implement GateProfiles and GateEngine (so every preset becomes consistent and testable).

Implement the Document visual gate (doc_visual_score), this will remove the worst false acceptances.

Flip Screenshots to metadata-only.

Tighten Wedding and Party with min_face_count.

Only then revisit weights, because weights are useless if the candidate set is polluted.

Below is a concrete, step by step plan to implement “smart gates” properly, using the files you pasted, and aligned with what Apple Photos, Google Photos, Lightroom, and Excire effectively do in practice (hybrid retrieval, then strong category specific gating, then ranking).

Your current architecture already supports this, because presets can carry intent (Documents, Screenshots, Wedding, Mountains), and the orchestrator already has a single scoring and dedup path. The missing piece is a consistent, explicit gate layer that runs before scoring and before top_k slicing.

1) What is going wrong right now
A) Documents vs non-documents cannot be separated by CLIP on ViT-B/32

You already saw the overlap in scores, a portrait and a paper can sit in the same similarity band, so threshold tuning alone will always fail at scale. Your Documents preset currently uses CLIP prompts plus negative prompts and exclude_faces=True. 

smart_find_service

That is the correct direction, but you still need one more discriminator: “does this image contain a lot of readable text-like structure”, which the big apps get from OCR and document classifiers.

B) Screenshots must be primarily metadata and heuristic gated, not CLIP gated

You already have screenshot detection in the orchestrator metadata cache via _detect_screenshot() and known device resolutions. 

search_orchestrator


But your Screenshots preset is currently defined as semantic prompts only, no filters. 

smart_find_service


That leaves you exposed to false positives and misses.

C) People implied categories (Wedding, Party, Baby, Portraits) need a face gate as a first class constraint

You implemented face_count loading from face_crops (good), and you log people implied coverage, also good.
But the “people implied” logic is still mostly a ranking boost, it should also be usable as a hard gate when precision matters.

2) The best practice pattern you should implement

All four products (Apple, Google, Lightroom, Excire) follow a pattern like this:

Retrieve candidates (semantic, metadata, or both)

Apply category gates (hard exclude, hard include)

Apply category rerankers (soft boosts)

Diversify and deduplicate

Return stable UI result set (no jitter, no duplicates)

You already have (1), (4), (5) partly. You need to formalize (2) and (3).

3) Add an explicit “GateProfile” to presets (smart_find_service.py)
Step 3.1, Extend preset schema

In BUILTIN_PRESETS, add a new key, for example gate_profile, and keep it purely declarative.

Example for Documents and Screenshots:

# in services/smart_find_service.py inside BUILTIN_PRESETS

{
  "id": "documents", "name": "Documents", "icon": "📄",
  "prompts": ["scanned document", "printed page", "form", "invoice", "receipt", "handwritten note"],
  "negative_prompts": ["screenshot", "phone screen", "app interface"],
  "category": "media",
  "semantic_weight": 0.4,
  "allow_backoff": False,
  "gate_profile": {
      "exclude_faces": True,
      "exclude_screenshots": True,
      "require_text_signal": True,
      "min_text_score": 0.35
  }
},
{
  "id": "screenshots", "name": "Screenshots", "icon": "📱",
  "prompts": ["screenshot", "screen capture", "phone screen"],
  "category": "media",
  "semantic_weight": 0.2,
  "gate_profile": {
      "require_screenshot": True
  }
},

Why this is correct in your codebase:

Your Documents preset already encodes the intent (exclude_faces) but only at the preset level, you want it enforced in orchestrator consistently.

Your orchestrator already computes is_screenshot and face_count in _get_project_meta(). 

search_orchestrator

Step 3.2, Do the same for people implied presets

For Wedding/Party:

"gate_profile": {
  "require_faces": True,
  "min_face_count": 1
}

Your presets for wedding, party exist here. 

smart_find_service

4) Implement the gate engine in SearchOrchestrator (search_orchestrator.py)

You want one function that takes candidates and a gate_profile, and returns filtered candidates plus explainability counters.

Step 4.1, Add _apply_gates() in SearchOrchestrator

Place it near other post processing utilities like _enforce_unique_paths. 

search_orchestrator

Conceptually:

Inputs:

paths: List[str]

project_meta: Dict[path, meta] (already available)

gate_profile: Dict

Output:

filtered list

gate stats for logging (dropped_by_face, dropped_by_screenshot, dropped_by_text)

Hard gates to support immediately using current metadata:

exclude_faces, require_faces, min_face_count

exclude_screenshots, require_screenshot (use meta["is_screenshot"])

require_gps (use meta["has_gps"]) if you want location dependent presets later

min_width, min_height, orientation

Step 4.2, Wire it into every orchestrator entry point

You already did a good job ensuring uniqueness across multiple search paths. 

search_orchestrator


Now do the same with gates:

In _execute() after you build merged candidates and before scoring, apply gates

In search_metadata_only() apply gates after metadata filter result list is built 

search_orchestrator

In search_ann() apply gates after ANN candidate retrieval

In find_similar() apply gates optionally (often none)

This is the single biggest “quality jump” because it aligns all entry points (presets, text, combined) to one behavior, like you already did for ranking.

Step 4.3, Feed gate_profile from preset into orchestrator

Right now, SmartFindService routes into orchestrator search methods. 

smart_find_service


So, update orchestrator’s preset resolution (where you turn preset_id into plan) to also pass gate_profile.

If your orchestrator currently reads preset configs from SmartFindService internally, then add a method in SmartFindService like:

get_preset_config(preset_id) returning the dict with gate_profile

Then orchestrator calls it once.

5) The missing module for Documents quality: “Text signal” (OCR-lite first, OCR full next)

Your require_text_signal gate cannot be real without a signal. You have two staged options:

Option 1 (fast, no OCR), implement a cheap “documentness” heuristic

This mimics what many systems do before OCR:

Downscale image, compute edge density and horizontal/vertical line strength (printed text has lots of short edges)

Compute low color variance (documents are often low saturation)

Compute “large uniform background + high contrast blobs”

This can be done with OpenCV quickly and cached per photo.

Where to put it:

New file: services/document_signal_service.py

Add a DB table or extend metadata cache:

simplest: store doc_signal in a new table keyed by photo_id or path

Then in _get_project_meta() enrich meta with doc_signal like you already do for is_screenshot and face_count. 

search_orchestrator


Now your gate require_text_signal uses meta["doc_signal"] >= min_text_score.

Option 2 (best practice), implement OCR indexing

This is what Apple/Google actually rely on heavily for Documents:

Create photo_ocr table: photo_id, text, char_count, avg_confidence, language, updated_at

Background job that OCRs photos incrementally (only new or changed)

Gate: require char_count >= N and avg_confidence >= X

If you do this, Documents becomes “precision-first” in the same way those products behave.

6) Category specific recommendations (what gates each preset should use)

You asked “for Document finding this path, for screenshot this path, for mountains, for wedding, etc”.

Use this as your default GateProfile library:

Documents

Hard gates:

exclude_faces = True

exclude_screenshots = True

require_text_signal = True (doc_signal or OCR)
Soft boosts:

boost if aspect ratio resembles A4-ish rectangle (portrait orientation)

boost if high resolution
Negative prompts:

already present (good) 

smart_find_service

Screenshots

Hard gates:

require_screenshot = True (your _detect_screenshot) 

search_orchestrator


Soft boosts:

boost if filename contains “Screenshot”, “Bildschirmfoto”, “Screen Shot”

boost if dimension exactly matches known pairs

Also, reduce semantic_weight (metadata wins here).

Wedding, Party, Baby, Portraits

Hard gates:

require_faces = True, min_face_count >= 1
Soft boosts:

boost for face_count > 2 (group photos)

boost for high “people implied” keywords in query, you already detect people implied in orchestrator logs, formalize it 

search_orchestrator

Mountains, Beach, Forest, Lake

Hard gates:

none, because landscape photos can also include people
Soft boosts:

boost if has_gps = True (location tends to correlate with travel scenes)

boost if orientation landscape, width_min threshold for panoramas you already do for Panoramas 

smart_find_service

City, Architecture

Soft boosts:

boost if GPS present

boost if higher resolution (architecture details)

7) Where exactly to modify in each file you pasted
A) services/smart_find_service.py

Add gate_profile to builtin presets (Documents, Screenshots, Wedding, Party, Portraits, Baby, etc).

When building a search request that routes to orchestrator, include gate_profile in the call (either as extra_filters or as a new param). 

smart_find_service

If you allow custom presets, extend their config_json schema to optionally store gate_profile too (future proof).

B) services/search_orchestrator.py

Implement _apply_gates(paths, gate_profile, project_meta) using face_count and is_screenshot already stored in meta. 

search_orchestrator

Ensure _execute(), search_metadata_only(), search_ann(), find_similar() call _apply_gates() before scoring and before slicing. 

search_orchestrator

Keep logs like:

“Gates applied: dropped_by_face=12, dropped_by_screenshot=3, kept=15”
This is exactly the kind of explainability you already do for scoring. 

search_orchestrator

Add a regression test for each gate, similar to how you increased coverage before.

C) services/search_service.py

You did not paste its content in the excerpts I can cite, but based on how SmartFindService calls _run_metadata_filter() and your TokenParser sets filters like flag="pick", media_type, has_gps, this file should be the one that:

translates filters into SQL constraints

should be extended for:

is_screenshot filter (use the same heuristic output, or store it in DB)

min_face_count filter (join to face_crops grouping)

Your NL parser already maps favorites to flag="pick" inside smart_find_service.py, that is correct. 

smart_find_service

D) services/search_history_service.py

Mostly fine, but once you introduce gate profiles, store them in the filters_json when recording searches, so the system can learn from “successful searches per gate type”, later powering suggestions.

8) What I would implement next, in priority order

If you want visible improvement fast, do it in this order:

Screenshots become hard gated by require_screenshot

Documents become hard gated by exclude_faces, exclude_screenshots, plus a minimal “doc_signal”

Wedding/Party become hard gated by require_faces, not just boosted

Add one “Not a document” action in UI, store it, and use it as negative training data for doc_signal thresholds (simple relevance feedback, you already have an early skeleton for events). 

search_orchestrator

If you want, I can now do the next concrete step in the most mechanical way: I will open your pasted search_orchestrator.py and propose an exact _apply_gates() implementation plus the exact call sites to patch (function names, placement, and the minimal tests to add), sticking strictly to your current meta fields (is_screenshot, face_count, width, height, has_gps, flag).
